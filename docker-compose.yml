version: "3.9"
services:
  web:
    build:
      context: .
      args:
        - VERSION
    ports:
      - "4202:4202"
    volumes:
      - /home/andreas/.config/:/root/.config/:z
      - /etc/letsencrypt/:/etc/letsencrypt/:z
      - /srv/verbal/media/:/srv/verbal/media/:z
